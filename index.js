// Declare and define the currentUser variable
const currentUser = "John Smith";

// Construct welcomeMessage using string concatenation
const welcomeMessage = "Welcome to Flatbook, " + currentUser + "!";

// Construct excitedWelcomeMessage using string concatenation and uppercase
const excitedWelcomeMessage = "WELCOME TO FLATBOOK, " + currentUser.toUpperCase() + "!";

// Construct shortGreeting using string concatenation and extracting the first initial
const shortGreeting = "Welcome, " + currentUser[0] + "!";
